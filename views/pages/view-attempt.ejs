<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Attempt</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500&display=swap" rel="stylesheet">
    <style>
        /* Global Styles */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #121212; /* Dark theme */
            color: #ffffff;
            margin: 0;
            padding: 0;
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 30px;
            background: #1E1E2E;
            box-shadow: 0 4px 10px rgba(255, 255, 255, 0.1);
        }

        .navbar a {
            color: #FFD700;
            text-decoration: none;
            font-weight: bold;
            margin-right: 15px;
            transition: 0.3s ease-in-out;
        }

        .navbar a:hover {
            color: #FFA500;
        }

        /* Admin Panel Layout */
        #admin-container {
            width: 90%;
            max-width: 1200px;
            margin: 40px auto;
            padding: 20px;
            background: #1E1E2E;
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(255, 255, 255, 0.1);
        }

        /* Section Titles */
        h1, h2 {
            text-align: center;
            color: #FFD700;
            text-transform: uppercase;
            margin-bottom: 20px;
        }

        /* Table Styles */
        .admin-table {
            width: 100%;
            border-collapse: collapse;
            background: #2A2A3A;
            border-radius: 10px;
            overflow: hidden;
            color: white;
            box-shadow: 0 5px 20px rgba(255, 255, 255, 0.1);
        }

        .admin-table thead {
            background: #FFD700;
            color: black;
        }

        .admin-table th, .admin-table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #555;
        }

        .admin-table tbody tr:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        /* Buttons */
        .btn {
            padding: 10px 15px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            text-transform: uppercase;
            font-weight: bold;
            transition: all 0.3s ease-in-out;
        }

        .btn-primary {
            background: #28A745;
            color: white;
        }

        .btn-primary:hover {
            background: #218838;
        }

        /* Back Button */
        .back-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border-radius: 5px;
            text-decoration: none;
        }

        .back-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

<header>
    <nav class="navbar">
        <a href="/admin/dashboard">Dashboard</a>
        <a href="/admin/questions">Quiz Questions</a>
        <a href="/users/logout">Logout</a>
    </nav>
</header>

<div id="admin-container">
    <h1>View Attempt</h1>

    <h2>Attempt by: <%= attempt.userId.username %></h2>
    <p><strong>Date:</strong> <%= new Date(attempt.attemptDate).toLocaleString() %></p>
    <p><strong>Score:</strong> <%= attempt.score %> / 5</p>

    <h3>Questions and Answers:</h3>
    <table class="admin-table">
        <thead>
            <tr>
                <th>Question</th>
                <th>Your Answer</th>
                <th>Correct Answer</th>
            </tr>
        </thead>
        <tbody>
            <% attempt.questions.forEach((question) => { %>
                <tr>
                    <td><%= question.questionId.question %></td>
                    <td><%= question.answer %></td>
                    <td><%= question.questionId.answers.find(a => a.isCorrect).text %></td>
                </tr>
            <% }); %>
        </tbody>
    </table>

    <a href="/admin/dashboard" class="back-btn">Back to Questions</a>
</div>

</body>
</html>
