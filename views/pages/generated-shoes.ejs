<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Shoe Generator</title>
    <link rel="stylesheet" href="/css/style.css">
    <script rel="/js/transik.js"></script>
    <script type="text/javascript" src="https://translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
</head>
<body>
    <header>
        <nav class="navbar">
            <a href="/">Home</a>
            <a href="/users/logout">Logout</a>
        </nav>
    </header>

    <main class="generated-shoes-container">
        <h1>👟 AI Shoe Generator</h1>
    
        <!-- Shoe Generation Form -->
        <section class="generate-shoe-section">
            <h2>🛠️ Generate Your Own Shoe</h2>
            <form id="generate-shoe-form" action="/generate/shoe" method="post">
                <label for="shoeIdea">Describe your shoe idea:</label>
                <textarea id="shoeIdea" name="description" placeholder="E.g. A futuristic sneaker with neon lights and a sleek design" required></textarea>
                <button type="submit" class="btn btn-primary">Generate Shoe</button>
            </form>
        </section>
    
        <!-- Display Generated Shoes -->
        <section class="generated-shoes-gallery">
            <h2>📸 My AI-Generated Shoes</h2>
            <% if (shoes.length > 0) { %>
                <div class="shoe-gallery">
                    <% shoes.forEach(shoe => { %>
                        <div class="shoe-card">
                            <!-- Ensure proper image format -->
                            <img src="data:image/png;base64,<%= shoe.imageUrl %>" alt="Generated Shoe">

                            <p><strong>Description:</strong> <%= shoe.description %></p>
                            <p><strong>Generated on:</strong> <%= new Date(shoe.createdAt).toLocaleString() %></p>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <p>No generated shoes yet! Create one now above. 👆</p>
            <% } %>
        </section>
        
    </main>
    
    <footer>
        <p>© 2025 AI Shoe Generator. All rights reserved.</p>
    </footer>

    <script>
        document.getElementById("generateShoeBtn").addEventListener("click", async function() {
            const description = document.getElementById("shoeIdea").value;
            if (!description) return alert("Please enter a shoe idea!");

            const response = await fetch("/generate/shoe", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ description })
            });

            const data = await response.json();
            if (data.success) {
                alert("✅ Shoe generated successfully!");
                location.reload();
            } else {
                alert("❌ Error generating shoe: " + data.message);
            }
        });
    </script>
</body>
</html>
